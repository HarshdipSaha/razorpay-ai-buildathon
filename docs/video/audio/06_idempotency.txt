Razorpay retries webhooks, so five of the sixty scenarios get their failure delivered twice, on purpose. The second delivery never reaches the classifier: the dedupe check runs first, and it's a database constraint on the event ID. Five duplicates in, zero double charges out.