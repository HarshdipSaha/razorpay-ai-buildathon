I almost shipped a bug that would have quietly escalated fifteen of sixty cases for no reason. I caught it by reading the gate's logic by hand, before I'd written a single line of code. Two smaller ones got through too: Windows choked on a character my own report tried to print, and an early version would have needed a real Razorpay login just to run offline. Here's what I built to make sure none of that ships silently again.
