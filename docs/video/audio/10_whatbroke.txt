Building this wasn't clean, and I'm not pretending it was. A Unicode crash on Windows. A design that would have secretly required a Razorpay login just to run the offline demo. And a one line policy bug that would have silently escalated a quarter of the batch, caught by hand tracing the logic during review, before a single line of code existed. All three are written up in FAILURES dot M D, because that's the part this buildathon says it reads first.