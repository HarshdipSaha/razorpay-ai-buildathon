Verify passes clean. Then I open the log file and change one byte by hand. Run verify again, and it fails on the exact record I touched. Each entry's hash depends on the one before it, so there's nowhere to hide a change.