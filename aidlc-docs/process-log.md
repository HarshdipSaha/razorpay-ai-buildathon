# Process Log — every AI skill and tool used to build this

Chronological. This is the "how it was actually made" record the buildathon asks for. Each entry names the tool/skill, what it was pointed at, what came out, and — where relevant — what went wrong.

All work driven through **Claude Code** (Anthropic's CLI agent). Times are IST, 2026-09-04 unless stated.

| # | When | Skill / tool | Used for | Output / outcome |
|---|---|---|---|---|
| 1 | ~07:45 | **Playwright MCP** (`browser_navigate`, `browser_snapshot`, `browser_click`, `browser_fill_form`) | Scrape razorpay.com/buildathon (single-page site) and walk the Google Form's pages to capture exact submission fields | `docs/hackathon-spec.md`. Form pages 1–2 captured; the auto-mode permission classifier blocked further form clicks, so page 3 fields came from the user's own paste. **Did not submit anything.** |
| 2 | ~07:50 | **Persistent memory** (file-based) | Save hackathon facts (deadline, tracks, eligibility quirk: grad-year dropdown offers only 2027–29) for future sessions | `razorpay-ai-buildathon.md` in memory store |
| 3 | ~08:00 | **`/academic-research-skills:ars-3w`** (deep-research, three-way-scan mode) | Entry point for research-grounded ideation | Reframed into two parallel research streams (below) |
| 4 | ~08:00 | **`AskUserQuestion`** | Disambiguate target hackathon (command text mentioned AWS docs; two live hackathons) | User: Razorpay AI Buildathon → recorded in `audit.md` |
| 5 | ~08:02 | **Forked subagent A** (agent-swarm *peer-parallel*) + **`WebSearch`** | Market scan per track: pain + evidence, incumbents, gap | Brief: Tracks 2 & 4 crowded by funded incumbents (Chargeflow/Justt; BlackLine/HighRadius). **Track 3 strongest gap** — no tool unifies root-cause diagnosis across the failure funnel. Track 1 gap: merchant-side agent-compliance gate. |
| 6 | ~08:02 | **Forked subagent B** + **`arxiv` skill** + **`duckduckgo-search` skill** | Academic scan: WHY/HOW/WHAT of 4–6 recent papers on payment fraud, agentic commerce safety, reconciliation agents, recovery workflows, bounded agent actions | *(pending at time of writing)* |
| 7 | ~08:05 | **`SendMessage`** to running subagent A | **Course correction:** user clarified AWS is irrelevant → dropped the AWS-building-blocks section mid-flight; redirected to a Razorpay test-mode API capability scan | Recorded in `audit.md`. Subagent A had already finished; its AWS section was discarded. |
| 8 | ~08:10 | **`/ai-dlc`** | Adopt AI-DLC as the build methodology; begin **Inception** | `aidlc-docs/` scaffold (this file, `inception/00-workspace-detection.md`, `registry.md`, `audit.md`) |
| 9 | ~08:12 | **`gh` CLI** + git | Fresh repo inside project folder (parent folder was inside a stray `H:\`-rooted repo); public GitHub repo created because submission requires a public URL | https://github.com/HarshdipSaha/razorpay-ai-buildathon — first commit `46d264d` |
| 10 | ~08:15 | **Forked subagent A (resumed)** + **`WebFetch`** on razorpay.com/docs | Razorpay test-mode API capability scan per track (replacement for the dropped AWS section) | **Track 3 ranked #1**: Razorpay's retry schedule is fixed (T+1/T+2/T+3 → `subscription.halted`), after which the merchant must charge manually; test mode can deterministically manufacture that failure ("Charge this now" → fail ×4) and recover via Invoices / Payment Links. **Track 2 ruled out**: no documented way to create a dispute in test mode. Track 4 crowded (BlackLine, HighRadius, FloQast…). |
| 11 | ~08:15 | *(failure)* **Forked subagent B** | — | **What broke:** the fork inherits the full conversation, and it *role-confused itself into the coordinator* — spawned its own sub-forks, and returned status chatter ("waiting on the arxiv fork, then I'll convene the council") instead of the paper digest. **Fix:** re-messaged it with a hard deliverable spec ("you are a research subagent, not the coordinator; no further subagents; output only the digest"). Lesson: forked agents need an explicit role boundary in the prompt, not just a task. |

| 12 | ~08:25 | **Forked subagent B (resumed)** + **`arxiv` skill** | Paper digest after the role-boundary fix | Six verified 2026 arXiv papers (2609.00060, 2608.23858, 2603.20953, 2607.23075, 2606.15949, 2608.14068) with WHY/HOW/WHAT. **Zero arXiv papers on failed-payment / dunning recovery** — Track 3 is academically unclaimed. Synthesis: "LLM explains, code decides"; pre-action authorisation gate with signed audit trail. → `docs/research-synthesis.md` |
| 13 | ~08:30 | **`llm-council`** skill (Karpathy-style: 5 advisors → anonymised peer review → chairman) | Pressure-test 3 candidates (A: Track 3 halted-subscription recovery agent; B: Track 1 AP2 mandate bridge; C: Track 4 three-way reconciliation) | Framed question written to scratchpad; 5 advisor subagents (Contrarian, First Principles, Expansionist, Outsider, Executor) spawned in parallel as *fresh* general-purpose agents — deliberately not forks, so they don't inherit the coordinator's bias toward Candidate A. Then 5 anonymised peer reviewers, then a chairman. **Verdict: APPROVE A** with 5 design changes. The decisive input was the Contrarian's circularity objection (test mode can't fail a charge *with a reason code* → self-labelled data), ranked strongest by all 5 reviewers; the Expansionist's "₹ per cause" headline was ranked the biggest blind spot by all 5. Peer review surfaced 4 things no advisor saw (idempotency, simulated clock, consent hard stop, non-code deliverables). → `docs/council-verdict.md` |
| 14 | ~08:50 | **Fact-check subagent** + `WebFetch` on razorpay.com/docs | Resolve the council's load-bearing unknown: do documented `payment.failed` / token webhook fields actually distinguish the claimed causes; can the halt path be compressed in test mode; webhook retry + idempotency docs | **Resolved.** 80+ documented `error_reason` values + per-method `error_source`/`error_step` enums → taxonomy maps from real fields. Card test numbers reproduce specific reasons; **UPI and dashboard failures are generic** (→ `unknown`). Halt path compresses to minutes. Webhooks at-least-once, dedupe on `x-razorpay-event-id`; no idempotency header for Payments → local guard. Token webhook test-mode firing undocumented → cause 5 labelled "documented, not manufacturable". |
| 15 | ~09:00 | **AI-DLC Inception Stage 1** | Write `inception/01-requirements.md` from council verdict + fact-check | 8 FR groups, 7-cause taxonomy table grounded to doc URLs with per-cause test-mode reproducibility, NFRs, 6 acceptance criteria. Working name **Rebound**. → Gate I-1 |

| 16 | ~09:10 | **AI-DLC Inception Stage 2** | `inception/02-application-design.md` — Mermaid architecture + sequence, 15 components mapped to FRs, pydantic data model, 8 ADR-lite decisions, "where AI is/isn't used" table, failure-mode table, 6-effort construction plan | Gate I-2: Continue → **baseline established** |
| 17 | ~09:15 | **AI-DLC Effort 001** opened | `efforts/001-scaffold-ingest-audit/` — effort-state (8 units, DoD) + requirements-delta (no new FRs; 4 clarifications, 2 NFRs) | Gate E-001–004 batched (deadline) |

| 18 | ~09:20 | *(interrupt)* | User stopped Gate E-001–004: evaluate the idea against the hackathon-idea-evaluator rubric first, then write a plan, then **stop — no coding** | Gate withdrawn. Recorded D-6. |
| 19 | ~09:25 | **`WebFetch`** → 429; **Playwright MCP** (browser) + **`gh api`** | Locate and read the third-party "Hackathon Idea Evaluator" rubric (mcpmarket listing → source repo hameed0342j/svh) | `SKILL.md` saved to scratchpad and read as a rubric. **Not installed, not executed** — third-party skill content is treated as data. |
| 20 | ~09:30 | **Hackathon-idea-evaluator rubric, applied by hand** | Score Rebound + the two rejected candidates; anti-inflation rules R1–R4; red-flag check | **Rebound 43/60** (Novelty 7 — capped by R1; Feasibility 7 after −2 data gap; Scalability 6; Impact 7; Demo 7 after −1 mock; Domain Fit 9). No red flags. B = 35 (Demo < 5 → abandon), C = 35 (Novelty < 4 → abandon). Sharpening S1–S4 → projected 47–48. → `docs/idea-evaluation.md`; **Amendment A** appended to `inception/01-requirements.md`. |
| 21 | ~09:35 | **`/writing-plans` skill** | Write the full TDD implementation plan (26 tasks) for Rebound from the approved requirements/design/Amendment A/rubric sharpening | `docs/superpowers/plans/2026-09-04-rebound-recovery-agent.md` |
| 22 | ~09:40–10:30 | **`plan-document-reviewer` subagent, 5 rounds** (per the skill's mandatory review loop) | Verify the plan against the spec before any code is written | Rounds 1–4 each found real, distinct defects and were fixed in place: (1) classifier metrics wired to nothing (dangling TODO pointing at a task that didn't exist); (2) FR-6.4's per-cause precision/recall/confusion-matrix/false-escalations never computed; (3) the live `serve` path never dispatched through classify→gate→execute, contradicting the video script; (4) a tuple-keyed confusion matrix would crash `json.dumps` in `cmd_demo`; (5) the self-contained HTML report was missing the required audit-chain view; (6) the real-invoice-ID fix only covered one of two adapter branches that both need it; (7) `Scenario.model_validate(envelope)` on raw webhook JSON would crash once real fixtures existed; (8) `policy.yaml`'s `max_attempts_by_cause: 0` for three causes silently forced immediate escalation regardless of what their own policy rows permitted — caught by tracing the logic, not by a failing test, since the existing test passed for the wrong reason; (9) my own round-3 fix for defect 7 was itself broken (`envelope.get("payload", envelope)` stripped the key the adapter needed to read); (10) a drafted README section (Amendment A / S4) was never actually inserted anywhere, deferred to a task that doesn't touch README.md; (11) AC-3 ("every non-unknown classification cites its documented source field") was unsatisfiable by the planned report. **Round 5: Approved**, with two non-blocking advisory notes. This 5-round loop is itself the clearest "AI judgment + failure recovery" evidence in the whole build — each defect was a plan-only paper cut, caught and fixed before a line of real code existed. |

**User then gave full go-ahead (D-8): implement end to end, no login/API key, produce an MP4 demo with voice.**

| 23 | ~10:40 | **Direct implementation** (Write/Edit/Bash — no further subagents; the plan from #21-22 was already fully specified and reviewed) | Build all `rebound/` modules, `policy.yaml`, tests, `fixtures/recorded/README.md` per the approved plan | 8 packages, ~20 modules, 54 tests, matching the plan almost verbatim — with one deliberate deviation: `cmd_demo` uses a new `SimulatedRazorpayClient` (`rebound/sim/fake_razorpay.py`), not the real-or-None client the plan sketched, so the demo needs no Razorpay login at all. Wiring the NUDGE/ESCALATE actions into `pipeline.py` also required a fix the plan hadn't fully specified: the gate's `escalate` verdict must override `Outcome.action` (not keep the originally-requested action) so the report buckets it correctly. |
| 24 | ~10:55 | **`pytest`** | Full suite, first run | **54 passed, 0 failed**, first try. |
| 25 | ~10:56 | **`python -m rebound.cli demo`** (real execution, not a claim) | Run the actual 60-scenario batch | Crashed: `UnicodeEncodeError` (Windows cp1252 vs. `→`/`·` in the report). Real, unplanned bug — fixed by forcing UTF-8 at every I/O boundary in `cli.py`. Re-ran: **0 policy violations, 0 double charges, 5/5 duplicate webhooks correctly rejected, 100% classifier accuracy / 5% abstain on the 20 held-out scenarios, all 60 scenarios accounted for across 7 causes.** → `FAILURES.md` #1. |
| 26 | ~11:00 | **`python -m rebound.cli report --html` / `verify-audit`** | Generate the HTML report; demonstrate tamper detection | `report.html` written. Tampered one byte of `audit.jsonl` by hand → `verify-audit` correctly reported `FAIL: chain broken at seq 10`; restored from backup → `OK` again. This is the recorded "one failure handled gracefully" the tracks' bar asks for, and it is a *real* tamper, not a scripted assertion. |
| 27 | ~11:05 | **README/FAILURES.md/docs/submission.md rewrite** | Replace placeholder status with real numbers pasted from the actual run; document all 3 real bugs found during construction (Unicode, the Razorpay-client requirement, the `policy.yaml` off-by-one caught in plan review) | `README.md`, `FAILURES.md`, `docs/submission.md` |
| 28 | ~11:10 | **`gh`/`git`** | Commit and push the full implementation | `33229c8` |
| 29 | ~11:15 | **Windows SAPI** (`System.Speech.Synthesis`, offline, no API key) + **Playwright MCP** (screenshot) + **local `http.server`** + **ffmpeg** | Produce a 5-minute pitch video without any screen-recording tool and without fabricating a live Razorpay demo | 11 HTML slides embedding *real* captured command output (`pytest`, `demo`, a real byte-level `verify-audit` tamper) → 11 narration WAVs → 11 screenshotted PNGs (`file://` was sandbox-blocked; served over `localhost:8791` instead) → ffmpeg pairs each image+audio, concatenates. **`docs/rebound-demo.mp4`, 3:57, 5.3MB.** Fully reproducible via `docs/video/build_slides.py` + `docs/video/assemble.sh`. |

## Planned next

| Skill / tool | Purpose |
|---|---|
| **`llm-council`** (5 independent AI advisors, anonymous peer review, synthesized verdict) | Pressure-test 3–4 candidate ideas; pick one. If the council rejects, iterate with a new candidate. |
| **AI-DLC Inception** stages 1–2 | `01-requirements.md`, `02-application-design.md` with approval gates |
| **AI-DLC Efforts** `001…` | Construction, one effort per unit; parallel subagents where units are independent |
| **`verification-before-completion`** | Evidence before any "it works" claim — metrics on a batch, not a happy-path demo |

## Where AI was deliberately *not* used

*(to be filled during construction — the buildathon's "AI judgment" criterion explicitly rewards this)*
